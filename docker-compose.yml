services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: growthtrail-backend
    ports:
      - "8081:8081"
    restart: unless-stopped

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
